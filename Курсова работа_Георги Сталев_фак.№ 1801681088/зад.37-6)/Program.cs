using System;

namespace зад._37_6_
{
    class Program
    {
        static void Main(string[] args)
        {
            //Ако със знаците (показани в учебника) изначим съответно операцийте отрицание,логическо събиране и логическо умножение,да се напише в C#
            //следния израз.
            Console.WriteLine("0.17 * Math.Pow(c, 3) - 1 <= 0 && z || (!z && t) && !v;");
        }
    }
}
